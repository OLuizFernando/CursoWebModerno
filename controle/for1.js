for (let i = 1; i <= 10; i++) { // for (declaração; condição; operação)
    console.log(`contador = ${i}`)
}

// percorrendo array apenas com 'for'
const nomes = ['Luiz', 'Fernando', 'Simone', 'Marlene', 'Nina']
for (let i = 0; i < nomes.length; i++) {
    console.log(`nomes[${i}] = ${nomes[i]}`)
}